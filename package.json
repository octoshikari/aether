{
  "name": "aether",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest",
    "storybook": "pnpm --filter @aether/storybook dev",
    "build:storybook": "pnpm --filter @aether/storybook build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.11.0"
}